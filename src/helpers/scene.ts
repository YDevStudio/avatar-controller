import * as THREE from 'three';

/**
 * Crée une nouvelle scène Three.js.
 * Rien de spécial ici, juste une instanciation propre.
 * Tu peux ajouter un background par défaut ici si besoin plus tard.
 *
 * @returns Une instance vide de THREE.Scene prête à être configurée
 */
export function setupScene(): THREE.Scene {
    return new THREE.Scene();
}